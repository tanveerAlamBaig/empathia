import 'package:flutter/material.dart';

class HomePsych extends StatelessWidget {
  const HomePsych({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
